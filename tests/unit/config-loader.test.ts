import { describe,it,expect } from 'vitest';import {mkdtempSync,writeFileSync,mkdirSync}from'node:fs';import{tmpdir}from'node:os';import{join}from'node:path';import{loadConfig}from'../../src/config.js';
describe('loadConfig',()=>{it('prefers explicit then environment then cwd',()=>{const root=mkdtempSync(join(tmpdir(),'docraider-'));const cwd=join(root,'cwd');mkdirSync(cwd);const env=join(root,'env.yml'),explicit=join(root,'explicit.yml');writeFileSync(join(cwd,'config.yaml'),'server: { port: 1 }');writeFileSync(env,'server: { port: 2 }');writeFileSync(explicit,'server: { port: 3 }');expect(loadConfig({cwd,packageRoot:root,env:{DOCRAIDER_CONFIG:env},loadDotEnv:false}).config.server.port).toBe(2);expect(loadConfig({cwd,packageRoot:root,configPath:explicit,env:{DOCRAIDER_CONFIG:env},loadDotEnv:false}).config.server.port).toBe(3);});it('fails before resource construction for missing explicit config',()=>expect(()=>loadConfig({configPath:'/definitely/missing'})).toThrow(/not found/));});
